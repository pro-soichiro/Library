<h1>貸し出し一覧</h1>
<table>
  <thead>
    <tr>
      <th>貸出日</th>
      <th>利用者</th>
    </tr>
  </thead>

  <tbody>
    <% @book.rentals.each do |rental| %>
      <tr>
        <td><%= rental.rental_date %></td>
        <td><%= rental.user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>