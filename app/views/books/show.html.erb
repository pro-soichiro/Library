<p id="notice"><%= notice %></p>



<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @book.description %>
</p>

<p>
  <strong>Images:</strong>
  <% @book.images.each do |image| %>
    <%= image_tag image, class: "image" %>
  <% end %>
</p>

<p>
  <strong>samples:</strong>
  <%= link_to "サンプル#{@book.sample.filename}", url_for(@book.sample) if @book.sample.attached? %>
</p>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>

<%# @book.rentals.each do |rent| %>
  <%#= render "rentals/rental",rental: rent %>
<%# end %>

<%#= render partial: "rentals/rental",collection: @book.rentals %>
<%#= render partial: @book.rentals %>
<%= render "rentals/index" %>